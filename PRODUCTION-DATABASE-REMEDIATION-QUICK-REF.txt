# PRODUCTION DATABASE REMEDIATION — QUICK REFERENCE

**Investigation Document**: `SCCB-PROP-PRODUCTION-DATABASE-VALIDATION-1606.md`

---

## FINDINGS

| Finding | Status |
|---------|--------|
| RDS attached | ❌ NO |
| PostgreSQL configured | ❌ NO |
| SQLite configured | ✅ YES (fallback) |
| SQLite file exists | ❌ NO |
| BlogPost table exists | ❌ NO (DB not created) |
| Phase-A articles seeded | ❌ NO |

**Current database**: SQLite at `/var/app/current/db.sqlite3` (missing)

---

## YOUR DECISION REQUIRED

Choose ONE:

### ✅ OPTION A: Quick Fix (SQLite) — Next 2 hours
- Initialize SQLite database
- Seed 10 Phase-A articles
- Verify blog URLs return 200 OK
- Ready for GSC submission
- **Risk**: Data loss if instance auto-scales (acceptable for Phase-5)
- **Timeline**: 30 minutes

### ✅ OPTION B: Production Fix (RDS) — Next 24 hours
- Attach RDS PostgreSQL to EB environment
- Initialize database with migrations
- Seed Phase-A articles
- Enterprise-grade reliability
- **Risk**: None (AWS managed backups)
- **Timeline**: 2-3 hours
- **Cost**: ~$10-20/month

---

## IF YOU CHOOSE OPTION A (SQLite)

**Run these commands in order:**

```bash
# 1. Connect to production
eb ssh

# 2. Activate environment
source /var/app/venv/*/bin/activate

# 3. Go to app directory
cd /var/app/current

# 4. Create database & tables
python manage.py migrate

# 5. Seed Phase-A articles (all published)
python manage.py seed_knowledge_hub_phase_a --publish

# 6. Verify (optional)
python manage.py shell
>>> from content.models import BlogPost
>>> print(f"Published: {BlogPost.objects.filter(is_published=True).count()}")
>>> exit()

# 7. Exit SSH
exit
```

**Then verify production URLs:**
```bash
curl -I https://www.propertism.in/en/blog/nri-property-management-basics/
```

**Expected**: `HTTP/2 200`

---

## IF YOU CHOOSE OPTION B (RDS)

**Requirements**:
- AWS console access or EB CLI expertise
- Decision to commit to PostgreSQL long-term

**Steps** (detailed in full report):
1. Attach RDS to EB environment (AWS console)
2. EB auto-injects database variables
3. SSH and run same migration/seed commands
4. Plan backup strategy

---

## IMPORTANT NOTES

- ✅ **Code is deployed correctly** — no git push needed
- ✅ **URL routing is correct** — `/blog/<slug>/` routes properly
- ✅ **Views are correct** — BlogPost filtering works
- ❌ **Only missing**: Database initialization & data seeding
- ⚠️ **Option A is TEMPORARY** — plan RDS migration within 1 week

---

## YOUR NEXT ACTION

1. **Read full report**: `SCCB-PROP-PRODUCTION-DATABASE-VALIDATION-1606.md`
2. **Choose**: Option A or Option B
3. **Run commands** from quick reference above or full report
4. **Verify**: Production URLs return 200 OK
5. **Proceed**: GSC submission with validated blog URLs

---

**Investigation Date**: 2026-06-16  
**Status**: ⏳ **AWAITING YOUR DECISION**
