from django.db import models

# ChatMessage model has been moved to properties.models.ContactMessage
# This app now only handles the chat widget views
# All chat messages are stored in the ContactMessage model
