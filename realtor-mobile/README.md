# realtor-mobile

React Native mobile app for Realtor platform using Expo Router.

## Structure
- `app/` file-based routes (tabs + auth)
- `src/modules/` feature modules (`properties`, `users`, `search`)
- `src/services/api.ts` shared API client for Django backend

## Run
```bash
npm install
npm run start
```
